{
  "name": "myapp",
  "version": "1.0.0",
  "main": "app.ts",
  "scripts": {
    "generate": "prisma generate",
    "dev": "npm run generate && npx tsx app.ts",
    "dev-db-push": "npx prisma db push && npm run generate",
    "migrate": "npx prisma migrate dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.11.0",
    "express": "^5.1.0",
    "tslib": "^2.8.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.10",
    "prisma": "^6.11.0",
    "ts-node-dev": "^2.0.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}